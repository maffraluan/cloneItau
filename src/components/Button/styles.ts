import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    viewDateButton: {
        minWidth: 130,
        width: 'auto',
        height: 30,
        alignItems: 'center',
        justifyContent: 'center',

        borderRadius: 50,
        borderWidth: 2,
        borderColor: '#8a919c'
    },
    fontDate: {
        fontSize: 16,
    },
})

export { styles }